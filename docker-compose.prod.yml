
services:
  api:
    command: uvicorn src.app.main:app --host 0.0.0.0 --port 8000
    ports:
      - "8000:8000"

  worker:
    command: celery -A src.workers.image_worker.celery_app worker --loglevel=info
